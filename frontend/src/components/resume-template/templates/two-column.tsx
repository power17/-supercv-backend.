import { Avatar, contactValues, ModuleContent, modulesOf, profileData, ResumePaper, type TemplateProps } from './shared'

export function TwoColumnTemplate({ resume }: TemplateProps) {
  const { profile, values } = profileData(resume); const modules = modulesOf(resume)
  return <ResumePaper resume={resume} className="grid grid-cols-[35%_65%]"><aside className="min-h-[1123px] bg-[#16213e] p-8 text-white"><Avatar resume={resume} className="mx-auto mb-5 rounded-full border-2 border-white/30" /><h1 className="text-center text-2xl font-bold">{profile?.name || '你的姓名'}</h1><p className="mt-1 text-center text-sm text-blue-200">{values.jobIntention || '求职意向'}</p><div className="mt-8 space-y-3 text-xs text-zinc-300">{contactValues(resume).map((value) => <p className="break-all" key={value}>{value}</p>)}</div>{modules.filter((_, index) => index % 3 === 0).map((module) => <section className="mt-8" key={module.key}><h2 className="mb-3 border-b border-white/20 pb-1 text-sm font-bold text-white">{module.title}</h2><ModuleContent module={module} accent="#bfdbfe" /></section>)}</aside><main className="p-8">{modules.filter((_, index) => index % 3 !== 0).map((module) => <section className="mb-6" key={module.key}><h2 className="mb-3 border-b-2 border-[#16213e] pb-1 text-sm font-bold text-[#16213e]">{module.title}</h2><ModuleContent module={module} accent="#16213e" /></section>)}</main></ResumePaper>
}
